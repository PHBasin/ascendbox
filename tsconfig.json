{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "preserve",
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "isolatedModules": true,
    // Nothing here ever emits: Vite builds the app, and `scripts/` runs on Node's native TypeScript
    // stripping. Stating it in the config (not just on the `vue-tsc` CLI) is what lets ESLint's
    // project service agree with `type-check` on the flag below.
    "noEmit": true,
    // `scripts/` is executed by Node, which resolves ESM specifiers literally and therefore needs
    // the `.ts` extension spelled out in the import.
    "allowImportingTsExtensions": true,
    "skipLibCheck": true,
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "paths": {
      "@/*": ["./src/*"]
    },

    // --- Type-safety (catches runtime bugs) ---
    "strict": true,
    // Index/array access yields `T | undefined` → forces a guard before use.
    "noUncheckedIndexedAccess": true,
    // Every code path must return.
    "noImplicitReturns": true,
    // No implicit fallthrough between switch cases.
    "noFallthroughCasesInSwitch": true,
    // Dead code is an error, not a warning.
    "allowUnreachableCode": false,
    "allowUnusedLabels": false,

    // --- Hygiene (keeps the codebase clean) ---
    "noUnusedLocals": true,
    "noUnusedParameters": true,

    // --- Team conventions (consistency across contributors) ---
    // Type-only imports/exports must say `type` → deterministic, stripped output.
    "verbatimModuleSyntax": true,
    // Overriding a base member requires the `override` keyword.
    "noImplicitOverride": true,
    // Import paths must match the file's real casing (guards Linux/macOS drift).
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*.ts", "src/**/*.vue", "vite.config.ts", "scripts/**/*.ts"]
}
